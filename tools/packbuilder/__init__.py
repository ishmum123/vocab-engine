"""packbuilder: shared, language-configurable builder for vocab-engine packs.

    python3 -m packbuilder build --lang it --repo <language repo> [--stage ...]
    python3 -m packbuilder check --lang it --repo <language repo>
    python3 -m packbuilder scan  --lang it --repo <language repo>
    python3 -m packbuilder sample --lang it --repo <language repo> --seed 303

See README.md in this directory.
"""
